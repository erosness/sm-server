(module bt-player *

(import scheme chicken)

(define (bt-next) #f)
(define (bt-prev) #f)
(define (bt-pause) #f)
(define (bt-unpause) #f)
(define (bt-refresh) #f)
(define (bt-start-pair) #f)
(define (bt-end-pair) #f)
(define (bt-name) #f)
(define (bt-set-handler handler) #f)

)
